interface:
  display_name: "Codex Harness"
  short_description: "Decision, context, and execution workflow"
  default_prompt: "Use $codex-harness to decide direction, freeze project context, and execute with verification."
